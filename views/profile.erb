<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>お天気SNS-Profile</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class='profile-container'>
        <a href='/edit'>編集する</a>
        <p><img src="/img/<%= @user.icon %>" class="profile-icon"></p>
        <p><%= @user.name %></p>
        <p><a href="/follows/<%= @user.id %>">フォロー：<%= @follows.size %></a></p>
        <p><a href="/followers/<%= @user.id %>">フォロワー：<%= @followers.size %></a></p>
    </div>
    <% @profile.each do |content| %>
    <div class='post'>
        <h3><%= content.content %></h3>
        <% if content.picture %>
          <img src="/img/<%= content.picture %>" class="post-picture">
        <% end %>
        <p><%= @city %> </p>
        <p><%= content.created_at.strftime('%Y年%m月%d日 %H:%M') %></p>
    </div>
    <% end %>
</body>